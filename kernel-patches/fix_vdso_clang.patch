diff --git a/arch/arm64/kernel/vdso/Makefile b/arch/arm64/kernel/vdso/Makefile
index c39aa9b512..0dbb5292c9 100644
--- a/arch/arm64/kernel/vdso/Makefile
+++ b/arch/arm64/kernel/vdso/Makefile
@@ -26,12 +26,15 @@ ldflags-y := -shared -nostdlib -soname=linux-vdso.so.1 --hash-style=sysv \
 
 ccflags-y += $(DISABLE_LTO)
 
-ccflags-y := -fno-common -fno-builtin -fno-stack-protector -ffixed-x18
+ccflags-y := -fno-common -fno-builtin -fno-stack-protector -ffixed-x18 -g0
 ccflags-y += -DDISABLE_BRANCH_PROFILING
 
 
-CFLAGS_REMOVE_vgettimeofday.o = $(CC_FLAGS_FTRACE) -Os $(CC_FLAGS_SCS)
+CFLAGS_REMOVE_vgettimeofday.o = $(CC_FLAGS_FTRACE) -Os $(CC_FLAGS_SCS) -g
 KBUILD_CFLAGS			+= $(DISABLE_LTO)
+AFLAGS_vgettimeofday.o		:= -no-integrated-as
+AFLAGS_note.o			:= -no-integrated-as
+AFLAGS_sigreturn.o		:= -no-integrated-as
 KASAN_SANITIZE			:= n
 UBSAN_SANITIZE			:= n
 OBJECT_FILES_NON_STANDARD	:= y
